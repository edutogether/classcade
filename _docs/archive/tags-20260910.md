# 2026-09-10 — freeze 태그 정리 (COMMON_STANDARDS §23-6, 대표 지시 1회성 소급 적용)

이 저장소는 이 정리 전까지 freeze 태그 6개를 쌓아두고 있었다. §23-6 기준(저장소당 최대
3슬롯: ①최신 `-audited-100` ②진행 중 `pre-` 태그 ③가장 최근 구조 전환 직전 지점, 단
**"그대로 되돌리면 실제로 동작하는가"가 슬롯 글자보다 우선**)으로 재정리했다.

**삭제하지 않았다 — `refs/archive/tags/<원래이름>`으로 옮겼다.** `git tag`와 GitHub 목록에서는
안 보이지만 커밋은 그대로 살아 있고, `git checkout refs/archive/tags/<이름>`으로 언제든
돌아간다.

## 남긴 것 (1개)

대표님이 직접 기준을 좁혔다: "GitHub Pages 시절은 버려라 — 이미 성공해서 더 좋은 규칙으로
옮겨왔고 안착했다. **다시 짜지 않아도 되는 선에서 남길 중요한 것만 남겨.**" 판정 질문은
하나 — **"이 태그를 잃으면 다시 짜야 하는 것이 있는가."**

| 태그 | 역할 | 커밋 | 날짜 |
|---|---|---|---|
| `classcade-freeze-20260910-audited-100` | **복구용** — 지금 고정 지점, 종합감사 10/10 | `cbf2088` | 2026-09-10 |

## 옮긴 것 (5개)

| 태그 | 커밋 | 날짜 | 메시지 첫 줄 | 옮긴 이유 |
|---|---|---|---|---|
| `classcade-freeze-20260814` | `200a2d5` | 2026-08-14 | CLASSCADE 전시/시연 프리즈 지점 — 로딩 화면·헤더 글로우·NBTI 다시 탐색하기 인터루드까지 반영 완료 | GitHub Pages 시절 지점. 되돌리려면 이미 삭제된 배포 워크플로부터 복원해야 한다 |
| `classcade-freeze-20260817` | `07b9328` | 2026-08-17 | CLASSCADE freeze — 2026-08-17 | 위와 동일 |
| `classcade-freeze-20260823` | `f27cd7e` | 2026-08-23 | 외부 재감사(2026-08-23) 도메인 재claim 방어 가드 추가 후 프리즈 | 위와 동일 |
| `classcade-freeze-20260826` | `8a07ad2` | 2026-08-26 | 정밀감사 전 프리즈 지점 (20260826) | Firebase Hosting 이전(09-09) 직전 지점이라 §23-6 슬롯③ 문구("구조 전환 직전")에는 맞지만, **역시 GitHub Pages 시절이라 되돌리려면 이미 삭제된 배포 워크플로 복원이 먼저 필요하다** — "되돌아갈 지점"이 아니라 "되돌아가려면 먼저 공사를 해야 하는 지점"이라 판단해 옮겼다(§23-6 "그대로 되돌리면 실제로 동작하는가" 기준) |
| `classcade-freeze-20260909-firebase-migration` | `ba59acc` | 2026-09-09 | Firebase Hosting 이전 완료 지점 (승인 Bumm 9/9) | 처음에는 "같은 인프라라 가볍게 되돌아갈 수 있다"는 이유로 남기기로 했으나, 대표님이 직접 기준을 좁혀 옮겼다 — **이전이 이미 성공해서 안착했고, 이 태그를 잃어도 다시 짜야 하는 것이 없다.** 20260910-audited-100이 같은 인프라 위에서 더 검증된 상위 지점이라 20260909만의 고유한 복구 가치는 없다고 판단 |

## 근거

`git checkout refs/archive/tags/<이름>`으로 실제 복귀 가능함을 이 정리 직후 실행 확인했다
(아래 표 참고). `.githooks/pre-push`가 freeze 태그 삭제를 막으므로 이 정리에 한해
`--no-verify`로 push했다 — 상시 보호는 그대로 켜져 있다.
